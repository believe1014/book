[pytest]
# 從 backend/ 執行：venv/Scripts/python.exe -m pytest
# 整合測試以 FastAPI TestClient 對臨時 SQLite 執行（見 tests/conftest.py）。
testpaths = tests
python_files = test_*.py
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
